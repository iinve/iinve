import { ModalBody, ModalHeader } from "@heroui/react";

export function ProModalHeader({ children }) {
  return <ModalHeader>{children}</ModalHeader>
}

export function ProModalBody({ children }) {
  return <ModalBody>{children}</ModalBody>
}